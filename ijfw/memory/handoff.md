Milestone 2 (detection-only) implemented in bookmark/extension/ on top of M1. New pure modules: shared/cleanup.js (computeDuplicateGroups from records excluding soft-deleted; analyzeFolders + normalizeFolderName for empty-folder and same-name merge candidates, never auto-merge; isSoftDeleted), shared/backup.js (buildBackup/serializeBackup/isValidBackup, full chrome.bookmarks.getTree() export with schema/version/exportedAt, downloaded in popup via Blob URL with no extra permission), shared/link-checker.js (classify/checkUrl pure three-state: only 404/410=unreachable, 2xx=reachable, everything else including 401/403/429/5xx/redirect/CORS/timeout=could_not_check; createLinkCheckController is permission-gated via hasPermission, chunked+checkpointed+alarm-driven, never automatic, skips non-web and soft-deleted records). manifest.json adds optional_host_permissions:['&lt;all_urls&gt;'] (NOT regular permissions). scan-controller persists FOLDER_FINDINGS at startNewScan and passes to report.computeReport at finishScan; report adds DUPLICATE_GROUPS_LIST, EMPTY_FOLDERS/EMPTY_FOLDERS_LIST, SAME_NAME_MERGE/SAME_NAME_MERGE_LIST while preserving truthful M1 open-history metrics. Service worker adds backup-export, check-links, link-check-status handlers + link resume on alarm. Popup adds always-visible backup button, opt-in link-check section that calls chrome.permissions.request before fetching, and read-only lists for duplicate groups/empty folders/merge candidates/link results. All new user-facing copy lives in shared/constants.js (neutral). Tests: unit-tests.js new cleanup/backup/link-checker blocks; harness Part 8 (detection persisted in report == independent analysis) and Part 9 (permission gate + three-state execution). `node test/run-tests.js 3000 42` from extension: ALL PASS exit 0. NOT committed (per request).
